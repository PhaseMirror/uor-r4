[package]
name = "ace"
version = "0.1.0"
edition = "2021"

[dependencies]
num-rational = "0.4"
# Optional Kani verification harness – enabled with the `kani` feature.
kani = { git = "https://github.com/model-checking/kani.git", rev = "kani-0.67.0", optional = true }

[features]
kani = ["dep:kani"]
