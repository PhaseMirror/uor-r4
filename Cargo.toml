[dependencies]
num_rational = "0.4"
[package]
name = "uor_r4_adr"
version = "0.1.0"
edition = "2021"

[dependencies]
# No external dependencies required for core ADR definitions

[dev-dependencies]
# No external dev dependencies; Kani verification is conditionally compiled.
